<%= form_with(model: section, local: true) do |form| %>
  <% if section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :section_number %>
    <%= form.number_field :section_number, id: :section_section_number %>
  </div>

  <div class="field">
    <%= form.label :section_building %>
    <%= form.text_field :section_building, id: :section_section_building %>
  </div>

  <div class="field">
    <%= form.label :section_room %>
    <%= form.number_field :section_room, id: :section_section_room %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
